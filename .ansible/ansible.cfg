[defaults]
inventory = inventory.yml
roles_path = roles
stdout_callback = yaml
# Deploys read better when a task says what it did rather than dumping JSON.
display_skipped_hosts = False
interpreter_python = auto_silent

[ssh_connection]
pipelining = True
